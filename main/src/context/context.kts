import context.*

/**
 * Chapter 4 provide contexts. This is about providing context to the DSL
 * Provide context for variables
 * */
retail amount 91.32
add tax sales
add tax local
compute total price
/**
 * In this example the variable sales and local are freely referenced by the user,
 * but neither the variable nor their values are defined within the code snippet.
 * only constant literal is 91.32
 * */